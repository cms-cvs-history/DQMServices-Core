process test={
  path p ={tester}
  module tester = DQMReadFileExample
	{untracked string RootFileName = "test_playback.root"}
  include "DQMServices/Core/data/DQM.cfg"
  untracked PSet maxEvents = {untracked int32 input = 1}
  source = EmptySource {}
}
